{
  "name": "nest-react",
  "version": "1.0.0",
  "description": "npm install -D turbo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.13.2"
  },
  "workspaces": [
    "apps/*"
  ]
}
